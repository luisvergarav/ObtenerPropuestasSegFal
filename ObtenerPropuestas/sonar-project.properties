#Se puede equiparar con el groupId de Maven (Obligatorio)
sonar.projectKey=obtenerPropuestasEJB
#Se puede equiparar con el artifactId de Maven. Es el nombre que se va a most
rar el dashboard de Sonar. (Obligatorio)
sonar.projectName=obtenerPropuestasEJB
#Se puede equiparar a la versi�n de Maven. (Obligatorio)
sonar.projectVersion=0.0.1
 
#Indicamos todos los directorios donde haya c�digo Java. (Obligatorio)
sonar.sources=ObtenerPropuestas/src
 
#Indicamos todos los directorios donde haya tests (Opcional)
#tests=testDir1,testDir2
 
#Indicamos los directorios donde se almacenan los binarios del proyecto (Opci
onal)
#binaries=target/web
 
#Indicamos la ruta de las librer�as y directorios que contengan .class (Opcio
nal)
#libraries=path/to/library.jar,path/to/classes/dir
 
 
# Indicamos el lenguaje que queremos analizar (Por defecto, Java)
#sonar.language=java
 
# Podemos indicar otros par�metros
#my.property=value
